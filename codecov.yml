coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 2%
    patch: off

fixes:
  - "github.com/golid-ai/golid/backend/::backend/"

flags:
  backend:
    paths:
      - backend/
  frontend:
    paths:
      - frontend/src/
