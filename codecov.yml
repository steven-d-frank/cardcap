coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 2%
    patch: off

fixes:
  - "github.com/steven-d-frank/cardcap/backend/::backend/"

flags:
  backend:
    paths:
      - backend/
  frontend:
    paths:
      - frontend/src/
