# =============================================================================
# Cardcap Frontend — Vite Environment Variables
# =============================================================================
# Copy to frontend/.env and fill in values. Vite loads this automatically.
# All vars must be prefixed VITE_ to be exposed to the browser.
#
# Note: Most config is handled by app.config.ts (Vite proxy, build settings).
# Only create a .env if you need to override these defaults.
# =============================================================================

# API URL override (default: "" — uses the Vite proxy defined in app.config.ts)
# Set this in production or when running the frontend standalone against a remote API.
# VITE_API_URL=https://api.yourdomain.com

# Stripe publishable key (safe to expose — this is a public key, not the secret key)
# VITE_STRIPE_PUB_KEY=pk_test_...

# AG Grid license key (optional — required for enterprise features only)
# VITE_AG_GRID_LICENSE=
