# ==============================================================================
# Cardcap Frontend - Docker Development (OPTIONAL)
# ==============================================================================
# NOTE: This Dockerfile is only used when running:
#   docker compose --profile frontend-docker up
#
# For normal development, the frontend runs as a VS Code task inside the
# devcontainer with proper HMR. This Docker option exists for CI/testing only.
# ==============================================================================

FROM node:20-alpine

WORKDIR /app

EXPOSE 3000

CMD ["sh", "-c", "npm install && npm run dev -- --host 0.0.0.0 --port 3000"]
