# =============================================================================
# CARDCAP QA ENVIRONMENT
# =============================================================================
# Target: GCP project (QA Cloud Run services)
#
# Secrets (JWT_SECRET, DB_PASSWORD, MAILGUN_API_KEY, STRIPE_SECRET_KEY, etc.)
# are stored in GCP Secret Manager, not in this file.
# See: docs/infrastructure/devops.md → Secrets Management
# =============================================================================

ENVIRONMENT=qa

# --- GCP Configuration ---
GCP_PROJECT_ID=cardcap
REGION=us-central1

# --- Database (Cloud SQL - PostgreSQL) ---
DB_USER=cardcap_user

# --- Application ---
APP_NAME=Cardcap

# --- Frontend URL (for email links) ---
FRONTEND_URL=https://your-web-qa.run.app

# --- Email Service (Mailgun) ---
# MAILGUN_DOMAIN=your-mailgun-domain.mailgun.org

# --- Frontend pub key (not a secret — embedded in browser bundle) ---
# VITE_STRIPE_PUB_KEY=pk_test_...
